KERBALCHANGELOG
{
	modName = ROUtils
	license = CC-BY-NC-SA 4.0
	author = KSP-RO team
	website = github.com/KSP-RO/ROUtils
	
	VERSION
	{
		version = 1.1.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Add common DragCubeTool, now with caching in #1
			subchange = Add faster implementation of FloatCurve in #2. All credit goes to @gotmachine
			subchange = Add  and  which was originally written for Kerbalism. A simple object cache to lower GC overhead.
			subchange = Add Fast  (and a const version), add , add Swap
			subchange = Move Principia bit in KSPUtils to ModUtils
			subchange = Remove  logic from , deprecate  on 
			subchange = Various other data type additions and improvements
		}
	}
	VERSION
	{
		version = 1.0.1.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Make changes required for RP-1 use
			subchange = Add ContinuousLogger
			subchange = Move Principia utils around
		}
	}
	VERSION
	{
		version = 1.0.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Initial release of this modding support library (has no user-facing features, exists just as a dependency). Includes the following:
			subchange = Utility functions from RO and RP-1
			subchange = Datatypes (PersistentX) from RP-1
			subchange = Object Serializer (including to/from zip) from RP-1
			subchange = SingletonHost from RP-1
			subchange = Mod detection utils
			subchange = ResourceUnitInfo from RO (and Kerbalism)
		}
	}
}
